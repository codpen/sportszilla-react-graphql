import React from 'react';
import styles from './JoinButton.module.scss';

const JoinButton: React.FC = () => (
  <div className={styles.JoinButton} data-testid="JoinButton">
    JoinButton Component
  </div>
);

export default JoinButton;
